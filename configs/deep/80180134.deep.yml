## Deep Hook — example configuration
## Copy to deep.yml or pass to load_config() in your backend.
## Previous review context is passed at runtime (see run_review(..., previous_review=...)).

# Project language: python | flutter | typescript | javascript | go | rust | java | other
language: python

# Global guidelines applied to every file
guidelines:
  - "Follow project coding standards"
  - "All public functions must have type hints"
  - "No bare except; use specific exceptions"

# Per-file-pattern guidelines (glob patterns)
file_guidelines:
  - pattern: "*.py"
    guidelines:
      - "Use docstrings on all public functions"
      - "Prefer context managers for resource handling"
  - pattern: "tests/**"
    guidelines:
      - "Each test must have a clear assertion"
  - pattern: "src/api/**"
    guidelines:
      - "Validate input with Pydantic"
      - "Return proper HTTP status codes"

# LLM provider
llm:
  provider: anthropic   # openai | anthropic | ollama
  model: claude-sonnet-4-6
  temperature: 0.1
  # For Ollama (local):
  # base_url: http://localhost:11434
  # For OpenAI/Anthropic (set api_key here or via env OPENAI_API_KEY / ANTHROPIC_API_KEY):
  # api_key: sk-...

# Review limits
review:
  max_diff_lines: 3000

# Optional MCP servers — tools are discovered automatically via langchain-mcp-adapters.
# The caller can also use mcp_fetcher in run_review() for custom guideline fetching.
# decide.fyi — deterministic subscription decision notaries for US consumers (stateless).
mcp:
  servers: []
    # - name: decide-refund
    #   url: https://refund.decide.fyi/api/mcp
    #   description: "Refund notary — eligibility and decision tools for refunds."
    #   transport: streamable_http
    # - name: decide-cancel
    #   url: https://cancel.decide.fyi/api/mcp
    #   description: "Cancel notary — subscription cancellation decision tools."
    #   transport: streamable_http
    # - name: decide-return
    #   url: https://return.decide.fyi/api/mcp
    #   description: "Return notary — return eligibility and decision tools."
    #   transport: streamable_http
    # - name: decide-trial
    #   url: https://trial.decide.fyi/api/mcp
    #   description: "Trial notary — trial eligibility and decision tools."
    #   transport: streamable_http
